'use strict';

/**
 * onboarding-content service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::onboarding-content.onboarding-content');
